"""Wire-format helpers and schema sources for superweb-cluster.

Use this package as the top-level home for three protocol groups:

- ``wire.internal_protocol`` for cluster-internal control/data-plane models and codecs
- ``wire.external_protocol`` for client-facing and artifact-facing protocol models
- ``wire.discovery_protocol`` for mDNS/DNS-SD discovery helpers
"""
